# 🏭 Система управления складами

Полноценное веб-приложение для управления складами, арендой и хранением вещей с разделением ролей (администратор/сотрудник/клиент), real-time уведомлениями и современным интерфейсом.

## 🚀 Технологии

### Backend (C# / ASP.NET Core)

- **ASP.NET Core 8.0** - Web API
- **Entity Framework Core** - ORM для работы с БД
- **SQLite** - База данных (легко заменить на SQL Server)
- **SignalR** - Real-time уведомления
- **JWT** - Аутентификация
- **BCrypt** - Хеширование паролей

### Frontend (Next.js)

- **Next.js 14** - React фреймворк
- **TypeScript** - Типизация
- **Tailwind CSS** - Стилизация
- **Framer Motion** - Анимации
- **Zustand** - Управление состоянием с persist middleware
- **SignalR Client** - Real-time связь с сервером
- **Axios** - HTTP клиент
- **React Hot Toast** - Уведомления
- **Heroicons** - Иконки

## 📁 Структура проекта

```
iuliasah_sklad/
├── backend/                    # C# ASP.NET Core API
│   ├── Controllers/           # API контроллеры
│   │   ├── AuthController.cs      # Аутентификация
│   │   ├── AdminController.cs     # Управление пользователями
│   │   ├── OrdersController.cs    # Заказы
│   │   ├── RentalsController.cs   # Аренда складов
│   │   ├── ItemsController.cs     # Вещи на хранении
│   │   ├── CategoriesController.cs # Категории вещей
│   │   ├── StorageLocationsController.cs # Места хранения
│   │   ├── WarehousesController.cs # Склады
│   │   ├── NotificationsController.cs # Уведомления
│   │   └── HistoryController.cs   # История действий
│   ├── Models/                # Модели данных
│   │   ├── User.cs
│   │   ├── Order.cs
│   │   ├── Rental.cs              # Аренда
│   │   ├── Item.cs                # Вещи
│   │   ├── ItemMovement.cs        # История движения вещей
│   │   ├── Category.cs            # Категории
│   │   ├── StorageLocation.cs     # Места хранения
│   │   ├── Warehouse.cs
│   │   ├── Notification.cs
│   │   └── ActionHistory.cs
│   ├── Data/                  # База данных
│   │   └── AppDbContext.cs
│   ├── DTOs/                  # Data Transfer Objects
│   ├── Services/              # Бизнес-логика
│   │   ├── JwtService.cs
│   │   ├── NotificationService.cs
│   │   └── StorageReminderService.cs  # Напоминания об истечении сроков
│   ├── Hubs/                  # SignalR хабы
│   │   └── NotificationHub.cs
│   ├── Program.cs
│   └── appsettings.json
│
└── frontend/                  # Next.js приложение
    ├── src/
    │   ├── app/              # Страницы (App Router)
    │   │   ├── login/            # Страница входа
    │   │   ├── register/         # Страница регистрации
    │   │   ├── client/           # Панель клиента
    │   │   │   ├── orders/       # Мои заказы
    │   │   │   ├── new-order/    # Новая аренда склада
    │   │   │   ├── items/        # Мои вещи
    │   │   │   └── new-item/     # Сдать вещь на хранение
    │   │   ├── employee/         # Панель сотрудника
    │   │   │   ├── items/        # Управление вещами
    │   │   │   ├── orders/       # Заказы на обработку
    │   │   │   └── locations/    # Места хранения
    │   │   └── admin/            # Панель администратора
    │   │       ├── orders/       # Управление заказами
    │   │       ├── items/        # Все вещи
    │   │       ├── warehouses/   # Управление складами
    │   │       ├── locations/    # Места хранения
    │   │       ├── admins/       # Управление админами
    │   │       ├── employees/    # Управление сотрудниками
    │   │       ├── users/        # Управление клиентами
    │   │       └── history/      # История действий
    │   ├── components/       # React компоненты
    │   │   ├── AuthGuard.tsx     # Защита маршрутов + проверка сессии
    │   │   ├── DashboardLayout.tsx # Layout с навигацией
    │   │   └── UI.tsx            # UI компоненты (Card, StatCard, StatusBadge, и др.)
    │   └── lib/              # Утилиты
    │       ├── api.ts            # API клиент
    │       ├── store.ts          # Zustand store с persist
    │       └── signalr.ts        # SignalR клиент
    └── package.json
```

## 🔧 Установка и запуск

### Требования

- .NET 8.0 SDK
- Node.js 18+
- npm или yarn

### Backend

```bash
# Перейти в папку backend
cd backend

# Восстановить пакеты
dotnet restore

# Запустить сервер
dotnet run
```

Сервер запустится на `http://localhost:5000`

### Frontend

```bash
# Перейти в папку frontend
cd frontend

# Установить зависимости
npm install

# Запустить в режиме разработки
npm run dev
```

Приложение откроется на `http://localhost:3000`

## 👥 Роли пользователей

### Клиент

- Регистрация и вход в систему
- **Аренда складов** с выбором дат и предпросмотром стоимости
- **Сдача вещей на хранение** с указанием категории и сроков
- Просмотр своих вещей и заказов
- Запрос на выдачу вещей
- Получение real-time уведомлений о статусах
- Отдельная статистика по арендам и вещам на дашборде

### Сотрудник

- Приём вещей на склад
- Выдача вещей владельцам
- Управление местами хранения
- Просмотр статистики по арендам и вещам
- Обработка заказов

### Администратор

- Все функции сотрудника
- Управление всеми заказами и арендами
- Одобрение/отклонение заказов и аренд
- Создание, редактирование и удаление складов
- **Настройка тарифов** (ставка за день для складов и мест хранения)
- Управление пользователями (клиенты, сотрудники, админы)
- Полная история действий в системе (система, вещи, аренды)
- Статистика по всем направлениям

## 🔐 Тестовые данные

При первом запуске создаются:

**Администратор:**

- Email: `admin@warehouse.com`
- Пароль: `admin123`

**Сотрудник:**

- Email: `employee@warehouse.com`
- Пароль: `employee123`

**Тестовые склады:**

- Центральный склад (Москва) - 1500₽/день
- Северный склад (Санкт-Петербург) - 1200₽/день
- Южный склад (Краснодар) - 1000₽/день

## 📡 API Endpoints

### Аутентификация

- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Текущий пользователь

### Заказы

- `GET /api/orders/my` - Мои заказы (клиент)
- `GET /api/orders` - Все заказы (админ/сотрудник)
- `POST /api/orders` - Создать заказ
- `PUT /api/orders/{id}/status` - Изменить статус

### Аренда складов

- `GET /api/rentals/my` - Мои аренды (клиент)
- `GET /api/rentals` - Все аренды (админ/сотрудник)
- `POST /api/rentals` - Создать заявку на аренду
- `PUT /api/rentals/{id}/status` - Изменить статус аренды

### Вещи

- `GET /api/items/my` - Мои вещи (клиент)
- `GET /api/items` - Все вещи (админ/сотрудник)
- `GET /api/items/{id}` - Детали вещи
- `POST /api/items` - Сдать вещь на хранение
- `PUT /api/items/{id}` - Обновить вещь
- `PUT /api/items/{id}/intake` - Принять вещь (сотрудник)
- `PUT /api/items/{id}/release` - Выдать вещь (сотрудник)
- `PUT /api/items/{id}/request-release` - Запросить выдачу (клиент)

### Категории

- `GET /api/categories` - Список категорий
- `POST /api/categories` - Создать категорию (админ)

### Места хранения

- `GET /api/storage-locations` - Все места хранения
- `GET /api/storage-locations/available` - Свободные места
- `POST /api/storage-locations` - Создать место (админ)
- `PUT /api/storage-locations/{id}` - Обновить место (админ)

### Склады

- `GET /api/warehouses` - Список складов
- `POST /api/warehouses` - Создать склад (админ)
- `PUT /api/warehouses/{id}` - Обновить склад (админ)
- `DELETE /api/warehouses/{id}` - Удалить склад (админ)

### Уведомления

- `GET /api/notifications` - Список уведомлений
- `GET /api/notifications/unread-count` - Количество непрочитанных
- `PUT /api/notifications/{id}/read` - Отметить прочитанным
- `PUT /api/notifications/read-all` - Прочитать все

### История

- `GET /api/history` - Системная история (админ)
- `GET /api/history/items` - История вещей (админ)
- `GET /api/history/rentals` - История аренд (админ)
- `GET /api/history/stats` - Статистика (админ)

### Администрирование

- `GET /api/admin/users` - Список пользователей
- `POST /api/admin/users` - Создать пользователя
- `PUT /api/admin/users/{id}` - Обновить пользователя
- `DELETE /api/admin/users/{id}` - Удалить пользователя

### SignalR

- `/hubs/notifications` - Хаб для real-time уведомлений

## ✨ Особенности

- 🎨 Современный адаптивный UI с Tailwind CSS
- 🔄 Плавные анимации с Framer Motion
- 🔔 Real-time уведомления через SignalR
- 🔒 JWT аутентификация
- 💾 **Сохранение сессии** на 5 минут (persist в localStorage)
- 📱 Мобильная адаптация
- 🎯 Разграничение доступа по ролям (admin/employee/client)
- 📊 **Раздельная статистика** по арендам и вещам
- 📜 **Полная история** с вкладками (система, вещи, аренды)
- 💰 **Расчёт стоимости** аренды с предпросмотром
- ⏰ **Напоминания** об истечении сроков хранения
- ✅ Валидация форм

## 📝 База данных

Используется SQLite (файл `warehouse.db` создаётся автоматически).
Для продакшена рекомендуется SQL Server или PostgreSQL.

### Схема БД

```sql
Users             - Пользователи (id, email, fullName, passwordHash, role, isActive)
Warehouses        - Склады (id, name, address, description, status, dailyRate)
Orders            - Заказы (id, description, status, userId, warehouseId)
Rentals           - Аренда (id, description, status, startDate, endDate, dailyRate, totalCost, userId, warehouseId)
Items             - Вещи (id, name, uniqueCode, status, dailyStorageCost, intakeDate, plannedReleaseDate, ownerId, warehouseId, categoryId, storageLocationId)
ItemMovements     - История движения вещей (id, actionType, performedById, itemId, notes)
Categories        - Категории (id, name, icon)
StorageLocations  - Места хранения (id, section, shelf, cell, isOccupied, dailyRate, warehouseId)
Notifications     - Уведомления (id, title, message, type, userId, isRead)
ActionHistories   - История действий (id, actionType, description, userId)
```

## 🔧 Конфигурация

### Backend (appsettings.json)

```json
{
  "Jwt": {
    "SecretKey": "YourSuperSecretKeyForJWTTokenGeneration123!"
  },
  "ConnectionStrings": {
    "DefaultConnection": "Data Source=warehouse.db"
  }
}
```

### Frontend (.env.local)

```
NEXT_PUBLIC_API_URL=http://localhost:5000/api
NEXT_PUBLIC_SIGNALR_URL=http://localhost:5000/hubs/notifications
```

## 📄 Лицензия

MIT
